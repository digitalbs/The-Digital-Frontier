$(document).ready(function () {
   
   /*
    setTimeout(function () {
        var canvas = document.getElementById('navTutorial'),
        context = canvas.getContext('2d'),
        angle = 0,
        range = 250,
        xSpeed = 1,
        ySpeed = 0.05,
        x1Pos,
        y1Pos,
        x2Pos,
        y2Pos;

        canvas.width = $(window).width();
        canvas.height = 400;

        (function startDraw() {
            window.requestAnimationFrame(startDraw, canvas);
            x1Pos = canvas.width - 50;
            y1Pos = 20;
            context.beginPath();
            context.moveTo(x1Pos, y1Pos);
            x1Pos -= xSpeed;
            angle += ySpeed;
            y1Pos = 160 + Math.sin(angle) * range;
            context.lineTo(x1Pos, y1Pos);
            context.stroke();
        } ());
    }, 3000);

    $(window).resize(function () {
        canvas.width = $(window).width();
        canvas.height = 400;
    });
*/
});


